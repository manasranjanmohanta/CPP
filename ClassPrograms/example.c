#include <stdio.h>
#include <string.h>
#include <math.h>
#include <stdlib.h>

int main() 
{ 
    char ch;
    char s[50];
    char sen[50];
    
    scanf("%c",&ch);
    scanf("%s",s);
    gets(sen);
    
    printf("%c",ch);
    printf("\n");
    puts(s);
    puts(sen);
    

    /* Enter your code here. Read input from STDIN. Print output to STDOUT */    
    return 0;
}
